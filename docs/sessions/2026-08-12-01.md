# 2026-08-12 · #01 — Transporte e status no menu + identidade visual (Órbita)

Status: concluída
Início: 2026-08-12 07:49

## Objetivo

1. Menu (contexto do widget e bandeja): linha informativa com estado do player e nome
   da música, e botões de transporte ⏮ ⏯ ⏭ lado a lado.
2. Identidade visual do app: logo/ícone, favicon e aplicação da marca nas Preferências
   e no widget.

## Resumo

- Menu ganhou linha de status ("Tocando/Pausado — faixa", com ícone) e linha de
  transporte com três botões em view SwiftUI dentro de `NSMenuItem` — clicar não fecha
  o menu. Comandos roteados pelo `NowPlayingController` existente.
- Identidade: 4 conceitos apresentados em prancheta (artifact); usuário escolheu
  **D · Órbita** (play em anel de progresso ciano→violeta sobre grafite).
- Assets gerados e versionados em `Resources/icon/`: SVGs-fonte, `AppIcon.icns`
  (16→1024), glifo template da bandeja, favicon PNG 16/32/48 + ICO.
- Pipeline reproduzível: `scripts/gerar-icones.sh` + `scripts/render-svg.swift`
  (renderizador CoreSVG nativo, sem dependência externa).
- Marca aplicada: cabeçalho nas Preferências (ícone + nome + versão, que saiu do
  rodapé), bandeja com glifo template (fallback SF Symbol no binário solto), e
  marca-d'água de 11 px a 16% no canto inferior direito do widget (`BrandMark`,
  vetor SwiftUI, sem hit test).

## Decisões

- Transporte no menu como view custom lado a lado (não fecha o menu) — escolha do
  usuário entre duas opções apresentadas.
- Conceito de identidade **Órbita** — ver `DECISOES.md`.
- Renderização de ícones via AppKit/CoreSVG nativo, sem brew/rsvg — ver `DECISOES.md`.

## Arquivos tocados

- `Sources/MacMediaWidget/`: `AppMenuController.swift`, `MenuTransportView.swift`
  (novo), `BrandMark.swift` (novo), `TrayController.swift`, `PreferencesWindow.swift`,
  `ContentView.swift`, `App.swift`, `L10n.swift`
- `Resources/`: `icon/` (novo, SVGs + icns + favicon), `Info.plist` (ícone, versão
  1.14.0), `pt-BR.lproj/Localizable.strings` (5 chaves)
- `scripts/`: `gerar-icones.sh` (novo), `render-svg.swift` (novo), `build-app.sh`
  (copia assets)

## Próximo passo

Testar manualmente a 1.14.0 (instalada em `/Applications`): menu com status e botões
sem fechar, ícone no Finder, glifo na bandeja, cabeçalho das Preferências e
marca-d'água do widget (opacidade 0.16 em `ContentView` se precisar de ajuste).
Continua pendente o teste manual do formato 1×1 (1.13.0).

## Métricas

- Tempo: 0h28m ativo · 0h28m decorrido (0 pausas descontadas)
- Turnos: 127
- Tokens: 14.659.536 total (entrada 233 · saída 177.800 · cache write 372.147 · cache read 14.109.356)
- Custo: US$ 30,44 · R$ 157,09
