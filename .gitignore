# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Build output (bundle .app montado à mão / dmg)
dist/
*.app/

# macOS
.DS_Store

# Logs
*.log

# Env / segredos
.env
.env.local

# graphify — artefatos locais de máquina (paths absolutos / cache); os outputs
# do grafo (graph.json, graph.html, GRAPH_REPORT.md, manifest.json) são versionados.
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/cache/
